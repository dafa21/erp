{modalType === 'vitals' && (
          <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 bg-slate-900/50 flex items-center justify-center p-4 z-50 backdrop-blur-sm">
            <motion.div initial={{ opacity: 0, scale: 0.95, y: 40 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: 0.95, y: 20 }} transition={{ type: "spring", stiffness: 300, damping: 30 }} className="bg-white dark:bg-slate-900 rounded-2xl w-full max-w-xl shadow-xl overflow-hidden border border-slate-200 dark:border-slate-800 flex flex-col max-h-[90vh]">
              <div className="flex justify-between items-center p-4 border-b border-slate-100 dark:border-slate-800 bg-slate-50 dark:bg-slate-800 shrink-0">
                <h3 className="font-bold text-slate-800 dark:text-slate-100 flex items-center gap-2"><Activity className="w-5 h-5 text-indigo-600 dark:text-indigo-400" /> Pemeriksaan Vital: {editingItem?.name}</h3>
                <button onClick={() => setModalType('none')} className="text-slate-400 dark:text-slate-500 hover:text-slate-600 dark:hover:text-white transition-colors outline-none"><X className="w-5 h-5" /></button>
              </div>
              <form onSubmit={saveVitals} className="p-5 overflow-y-auto flex-grow custom-scrollbar bg-white dark:bg-slate-900">
                <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-4">
                  <VitalInputField 
                    label="Tensi Darah" 
                    unit="mmHg" 
                    icon={Activity} 
                    required 
                    value={vitalsForm.blood_pressure} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, blood_pressure: val})} 
                    placeholder="120/80" 
                  />
                  <VitalInputField 
                    label="Suhu Tubuh" 
                    unit="°C" 
                    icon={Thermometer} 
                    required 
                    type="number" 
                    step="0.1" 
                    value={vitalsForm.temperature} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, temperature: val})} 
                    placeholder="36.5" 
                  />
                  <VitalInputField 
                    label="Detak Nadi" 
                    unit="bpm" 
                    icon={Heart} 
                    required 
                    type="number" 
                    value={vitalsForm.heart_rate} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, heart_rate: val})} 
                    placeholder="80" 
                  />
                  <VitalInputField 
                    label="Napas" 
                    unit="x/menit" 
                    icon={Wind} 
                    required 
                    type="number" 
                    value={vitalsForm.respiratory_rate} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, respiratory_rate: val})} 
                    placeholder="18" 
                  />
                  <VitalInputField 
                    label="Saturasi Oksigen" 
                    unit="%" 
                    icon={Activity} 
                    required 
                    type="number" 
                    value={vitalsForm.oxygen_saturation} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, oxygen_saturation: val})} 
                    placeholder="98" 
                  />
                  <VitalInputField 
                    label="Skala Nyeri" 
                    unit="0-10" 
                    icon={AlertCircle} 
                    required 
                    type="number" 
                    min={0} 
                    max={10} 
                    value={vitalsForm.pain_score} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, pain_score: val})} 
                    placeholder="2" 
                  />
                  <VitalInputField 
                    label="Berat Badan" 
                    unit="kg" 
                    icon={Scale} 
                    required 
                    type="number" 
                    step="0.1" 
                    value={vitalsForm.weight} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, weight: val})} 
                    placeholder="65.5" 
                  />
                  <VitalInputField 
                    label="Tinggi Badan" 
                    unit="cm" 
                    icon={LayoutDashboard} 
                    required 
                    type="number" 
                    step="0.1" 
                    value={vitalsForm.height} 
                    onChange={(val: any) => setVitalsForm({...vitalsForm, height: val})} 
                    placeholder="170" 
                  />
                </div>

                <div className="mb-4 bg-indigo-50 dark:bg-indigo-900/10 p-4 rounded-xl border border-indigo-100 dark:border-indigo-900/30">
                   <div className="flex justify-between items-center mb-3">
                     <p className="text-xs font-bold text-indigo-900 dark:text-indigo-400 uppercase tracking-widest flex items-center gap-2">
                       <Activity className="w-4 h-4" /> AI Triage & Analisis Sensor
                     </p>
                     <button type="button" onClick={handleAiTriage} disabled={isAiTriageLoading} className="px-4 py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded-lg text-[10px] font-bold uppercase tracking-widest transition-all flex items-center gap-2 disabled:opacity-50">
                       {isAiTriageLoading ? <Loader2 className="w-3.5 h-3.5 animate-spin" /> : <Activity className="w-3.5 h-3.5" />}
                       {isAiTriageLoading ? 'Menganalisis...' : (vitalsForm.blood_pressure || vitalsForm.temperature || vitalsForm.heart_rate || vitalsForm.respiratory_rate || vitalsForm.oxygen_saturation ? 'Proses Triage AI' : 'Baca Sensor Pintar')}
                     </button>
                   </div>
                   {vitalsForm.triage_level && (
                     <div className={`p-3 rounded-lg border flex gap-3 ${vitalsForm.triage_level === 'Merah' ? 'bg-rose-50 border-rose-200 text-rose-800 dark:bg-rose-900/20 dark:border-rose-800 dark:text-rose-300' : vitalsForm.triage_level === 'Kuning' ? 'bg-amber-50 border-amber-200 text-amber-800 dark:bg-amber-900/20 dark:border-amber-800 dark:text-amber-300' : 'bg-emerald-50 border-emerald-200 text-emerald-800 dark:bg-emerald-900/20 dark:border-emerald-800 dark:text-emerald-300'}`}>
                        <div className="shrink-0 mt-0.5">
                           {vitalsForm.triage_level === 'Merah' ? <AlertCircle className="w-5 h-5 text-rose-500" /> : vitalsForm.triage_level === 'Kuning' ? <AlertTriangle className="w-5 h-5 text-amber-500" /> : <CheckCircle2 className="w-5 h-5 text-emerald-500" />}
                        </div>
                        <div>
                          <p className="font-bold text-xs uppercase tracking-widest mb-1">Triage: {vitalsForm.triage_level}</p>
                          <p className="text-xs leading-relaxed opacity-90">{vitalsForm.triage_notes}</p>
                        </div>
                     </div>
                   )}
                </div>

                <div className="pt-6 flex justify-end gap-3 border-t border-slate-100 dark:border-slate-800 shrink-0 bg-slate-50 dark:bg-slate-900 -mx-5 px-5 py-4">
                  <button type="button" onClick={() => setModalType('none')} className="px-6 py-2.5 bg-slate-100 dark:bg-slate-800 rounded-xl text-xs font-bold uppercase tracking-widest text-slate-600 dark:text-slate-400 hover:bg-slate-200 dark:hover:bg-slate-700 transition-all outline-none">Batal</button>
                  <button type="submit" className="px-8 py-2.5 bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-600 rounded-xl text-xs font-black uppercase tracking-widest text-white shadow-lg shadow-indigo-100 dark:shadow-none transition-all outline-none">
                    Simpan Tanda Vital
                  </button>
                </div>
              </form>
            </motion.div>
          </motion.div>
        )}
        </AnimatePresence>

                <ChildrenModals
          modalType={modalType}
          setModalType={setModalType as any}
          editingItem={editingItem}
          selectedChild={selectedChild}
          setSelectedChild={setSelectedChild}
          childForm={childForm}
          setChildForm={setChildForm}
          saveChild={saveChild}
          immunizationForm={immunizationForm}
          setImmunizationForm={setImmunizationForm}
          saveImmunization={saveImmunization}
          growthForm={growthForm}
          setGrowthForm={setGrowthForm}
          saveGrowth={saveGrowth}
          darkMode={darkMode}
          currentUser={currentUser}
          getChildImmunizationStatus={getChildImmunizationStatus}
          openNewChildModal={openNewChildModal}
        />
        {modalType === 'drug' && (
          <div className="fixed inset-0 bg-slate-900/50 flex items-center justify-center p-4 z-50 backdrop-blur-sm">
            <div className="bg-white dark:bg-slate-900 rounded-2xl w-full max-w-md shadow-xl overflow-hidden border border-slate-200 dark:border-slate-800">
              <div className="bg-emerald-600 p-4 text-white flex justify-between items-center">
                <h3 className="font-bold flex items-center gap-2"><Pill className="w-5 h-5"/> {editingItem ? 'Edit Obat' : 'Tambah Obat Baru'}</h3>
                <button onClick={() => setModalType('none')} className="text-white hover:text-emerald-200 transition-colors"><X className="w-5 h-5"/></button>
              </div>
              <form onSubmit={saveDrug} className="p-5 space-y-4 max-h-[80vh] overflow-y-auto custom-scrollbar">
                {currentUser?.role === 'Superadmin' && (
                   <div>
                     <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Tujuan Klinik / Lokasi Apotek</label>
                     <select value={drugForm.clinic_id} onChange={e => setDrugForm({...drugForm, clinic_id: e.target.value})} required className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm">
                       <option value="">Pilih Cabang Klinik...</option>
                       {clinicsInfo.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}
                     </select>
                   </div>
                )}
                <div>
                  <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Nama / Merk Obat</label>
                  <input required value={drugForm.name} onChange={e => setDrugForm({...drugForm, name: e.target.value})} type="text" className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm" placeholder="Misal: Paracetamol 500mg" />
                </div>
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Tanggal Produksi</label>
                    <input type="date" value={drugForm.mfg_date} onChange={e => setDrugForm({...drugForm, mfg_date: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm" />
                  </div>
                  <div>
                    <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Tanggal Kadaluarsa (Expired)</label>
                    <input type="date" value={drugForm.exp_date} onChange={e => setDrugForm({...drugForm, exp_date: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm" />
                  </div>
                </div>
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Satuan</label>
                    <select value={drugForm.unit} onChange={e => setDrugForm({...drugForm, unit: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm">
                      <option value="Tablet">Tablet</option>
                      <option value="Kapsul">Kapsul</option>
                      <option value="Botol">Botol (Sirup)</option>
                      <option value="Sachet">Sachet</option>
                      <option value="Ampul">Ampul</option>
                      <option value="Vial">Vial</option>
                      <option value="Pcs">Pcs / Lembar</option>
                    </select>
                  </div>
                  <div>
                    <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Total Stok</label>
                    <input required value={drugForm.stock} onChange={e => setDrugForm({...drugForm, stock: Number(e.target.value)})} type="number" min="0" className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm" />
                  </div>
                </div>
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Harga Beli per Satuan (HPP) (Rp)</label>
                    <input required value={drugForm.purchase_price} onChange={e => setDrugForm({...drugForm, purchase_price: Number(e.target.value)})} type="number" min="0" className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm" />
                  </div>
                  <div>
                    <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Harga Jual per Satuan (Rp)</label>
                    <input required value={drugForm.price} onChange={e => setDrugForm({...drugForm, price: Number(e.target.value)})} type="number" min="0" className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm" />
                  </div>
                </div>
                <div>
                  <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Akun COA Persediaan Obat (Aset)</label>
                  <select value={drugForm.inventory_coa_id} onChange={e => setDrugForm({...drugForm, inventory_coa_id: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm">
                    <option value="">-- Gunakan Default (1.1.3.01 - Persediaan Obat & Farmasi) --</option>
                    {coa.filter(a => a.account_code.startsWith('1') || a.account_type?.toLowerCase().includes('aset') || a.account_type?.toLowerCase().includes('aktiva')).map(a => (
                      <option key={a.id} value={a.id} disabled={a.level !== 'Child'} className={a.level !== 'Child' ? 'font-bold bg-slate-100 text-slate-500' : 'pl-4 text-slate-800'}>
                        {a.account_code} - {a.account_name}
                      </option>
                    ))}
                  </select>
                </div>
                <div>
                  <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-2">Akun COA Pendapatan Farmasi (Obat)</label>
                  <select value={drugForm.revenue_coa_id} onChange={e => setDrugForm({...drugForm, revenue_coa_id: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 focus:ring-2 focus:ring-emerald-500 focus:outline-none dark:text-white transition-all shadow-sm">
                    <option value="">-- Gunakan Default (4.2.1.01 - Pendapatan Farmasi) --</option>
                    {coa.filter(a => a.account_code.startsWith('4') || a.account_type?.toLowerCase().includes('pendapatan')).map(a => (
                      <option key={a.id} value={a.id} disabled={a.level !== 'Child'} className={a.level !== 'Child' ? 'font-bold bg-slate-100 text-slate-500' : 'pl-4 text-slate-800'}>
                        {a.account_code} - {a.account_name}
                      </option>
                    ))}
                  </select>
                </div>
                <div className="flex justify-end gap-3 pt-4 border-t border-slate-100 dark:border-slate-800">
                  <button type="button" onClick={() => setModalType('none')} className="px-5 py-2.5 bg-slate-100 dark:bg-slate-800 rounded-xl text-[10px] font-black uppercase tracking-widest text-slate-600 dark:text-slate-400 hover:bg-slate-200 dark:hover:bg-slate-700 transition-all">Batal</button>
                  <button type="submit" className="px-6 py-2.5 bg-emerald-600 hover:bg-emerald-700 text-white rounded-xl text-[10px] font-black uppercase tracking-widest shadow-lg shadow-emerald-100 dark:shadow-none transition-all">{editingItem ? 'Simpan' : 'Tambah Obat'}</button>
                </div>
              </form>
            </div>
          </div>
        )}

        <AnimatePresence>
        {modalType === 'soap' && (
          <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 bg-slate-900/50 flex items-center justify-center p-4 z-50 backdrop-blur-sm">
            <motion.div initial={{ opacity: 0, scale: 0.95, y: 40 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: 0.95, y: 20 }} transition={{ type: "spring", stiffness: 300, damping: 30 }} className="bg-white dark:bg-slate-900 rounded-2xl w-full max-w-2xl shadow-xl overflow-hidden border border-slate-200 dark:border-slate-800 flex flex-col max-h-[90vh]">
              <div className="flex justify-between items-center p-4 border-b border-slate-100 dark:border-slate-800 bg-slate-50 dark:bg-slate-800 shrink-0">
                <h3 className="font-bold text-slate-800 dark:text-slate-100 flex items-center gap-2 uppercase tracking-tight text-sm"><Stethoscope className="w-5 h-5 text-indigo-600 dark:text-indigo-400" /> Rekam Medis (SOAP): {editingItem?.name}</h3>
                <button onClick={() => setModalType('none')} className="text-slate-400 dark:text-slate-500 hover:text-slate-600 dark:hover:text-white transition-colors outline-none"><X className="w-5 h-5" /></button>
              </div>
              
              <div className={`px-5 py-3 border-b shrink-0 flex justify-between tracking-tight text-[10px] font-bold uppercase transition-colors ${editingItem?.vitals?.triage_level === 'Merah' ? 'bg-rose-100 dark:bg-rose-900/40 border-rose-200 dark:border-rose-900/50 animate-[pulse_2s_ease-in-out_infinite] shadow-[inset_0_0_15px_rgba(244,63,94,0.1)]' : editingItem?.vitals?.triage_level === 'Kuning' ? 'bg-amber-50 dark:bg-amber-900/20 border-amber-200 dark:border-amber-900/50' : 'bg-indigo-50 dark:bg-indigo-900/20 border-indigo-100 dark:border-indigo-900/30'}`}>
                <div className="flex items-center gap-3">
                  {editingItem?.vitals?.triage_level === 'Merah' && (
                    <div className="flex items-center gap-1 text-rose-600 dark:text-rose-400">
                       <AlertCircle className="w-4 h-4 animate-bounce" />
                       <span className="font-black text-[11px] tracking-widest">DARURAT (MERAH)</span>
                    </div>
                  )}
                  {editingItem?.vitals?.triage_level === 'Kuning' && (
                    <div className="flex items-center gap-1 text-amber-600 dark:text-amber-400">
                       <AlertTriangle className="w-4 h-4" />
                       <span className="font-black">PERHATIAN (KUNING)</span>
                    </div>
                  )}
                  <div>
                    <span className={editingItem?.vitals?.triage_level === 'Merah' ? 'text-rose-800 dark:text-rose-300' : editingItem?.vitals?.triage_level === 'Kuning' ? 'text-amber-800 dark:text-amber-300' : 'text-indigo-800 dark:text-indigo-400'}>TTV Terakhir: </span>
                    {editingItem?.vitals ? (
                       <span className={editingItem?.vitals?.triage_level === 'Merah' ? 'text-rose-600 dark:text-rose-200' : editingItem?.vitals?.triage_level === 'Kuning' ? 'text-amber-600 dark:text-amber-200' : 'text-indigo-600 dark:text-indigo-300'}>BP: {editingItem.vitals.blood_pressure} | Temp: {editingItem.vitals.temperature}°C | Nyeri: {editingItem.vitals.pain_score}</span>
                    ) : <span className="text-slate-500 dark:text-slate-400 italic lowercase font-medium">Belum ada data TTV</span>}
                  </div>
                </div>
                <div>
                  <span className={editingItem?.vitals?.triage_level === 'Merah' ? 'text-rose-800 dark:text-rose-300' : editingItem?.vitals?.triage_level === 'Kuning' ? 'text-amber-800 dark:text-amber-300' : 'text-indigo-800 dark:text-indigo-400'}>Keluhan Triage:</span> <span className={editingItem?.vitals?.triage_level === 'Merah' ? 'text-rose-600 dark:text-rose-200' : editingItem?.vitals?.triage_level === 'Kuning' ? 'text-amber-600 dark:text-amber-200' : 'text-indigo-600 dark:text-indigo-300'}>{editingItem?.complaint || '-'}</span>
                </div>
              </div>

              <form onSubmit={saveSoap} className="p-5 overflow-y-auto flex-grow custom-scrollbar space-y-5 bg-white dark:bg-slate-900">
                {selectedPatientImages.length > 0 && (
                  <div>
                    <label className="block text-[10px] font-black text-slate-700 dark:text-slate-200 mb-2 flex items-center gap-1 uppercase tracking-widest"><ImageIcon className="w-4 h-4 text-indigo-500" /> Foto / Dokumen Awal Pasien Masuk</label>
                    <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
                      {selectedPatientImages.map((img, idx) => (
                        <a key={idx} href={img.image_data} target="_blank" rel="noopener noreferrer" className="block relative aspect-video rounded-xl overflow-hidden hover:opacity-90 ring-1 ring-slate-200 dark:ring-slate-700 transition-opacity shadow-sm">
                          <img src={img.image_data} alt={`File ${idx + 1}`} className="w-full h-full object-cover" />
                        </a>
                      ))}
                    </div>
                  </div>
                )}
                <div>
                  <label className="block text-[10px] font-black text-slate-700 dark:text-slate-200 mb-1 flex items-center gap-1 uppercase tracking-widest"><span className="bg-indigo-100 dark:bg-indigo-900 text-indigo-800 dark:text-indigo-300 px-1.5 rounded text-[9px]">S</span> Subjective (Subjektif)</label>
                  <p className="text-[9px] text-slate-500 dark:text-slate-400 mb-2 uppercase font-bold tracking-tight">Keluhan utama, riwayat penyakit, gejala yang dirasakan pasien.</p>
                  <textarea required value={soapForm.subjective} onChange={e => setSoapForm({...soapForm, subjective: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[80px] transition-all shadow-sm" placeholder="Misal: Pasien mengeluh demam sejak 3 hari yang lalu..." />
                </div>
                <div>
                  <label className="block text-[10px] font-black text-slate-700 dark:text-slate-200 mb-1 flex items-center gap-1 uppercase tracking-widest"><span className="bg-indigo-100 dark:bg-indigo-900 text-indigo-800 dark:text-indigo-300 px-1.5 rounded text-[9px]">O</span> Objective (Objektif)</label>
                  <p className="text-[9px] text-slate-500 dark:text-slate-400 mb-2 uppercase font-bold tracking-tight">Hasil pemeriksaan fisik (raba, ketuk, dengar) dan penunjang (lab, rontgen).</p>
                  <textarea required value={soapForm.objective} onChange={e => setSoapForm({...soapForm, objective: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[80px] transition-all shadow-sm" placeholder="Misal: Tampak sakit sedang, suhu 38.5C, faring hiperemis..." />
                </div>
                
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label className="block text-[10px] font-black text-slate-700 dark:text-slate-200 mb-1 flex items-center gap-1 uppercase tracking-widest"><span className="bg-indigo-100 dark:bg-indigo-900 text-indigo-800 dark:text-indigo-300 px-1.5 rounded text-[9px]">A</span> Assessment (Asesmen)</label>
                    <p className="text-[9px] text-slate-500 dark:text-slate-400 mb-2 uppercase font-bold tracking-tight">Kesimpulan penyakit atau diagnosis.</p>
                    <textarea required value={soapForm.assessment} onChange={e => setSoapForm({...soapForm, assessment: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[80px] transition-all shadow-sm" placeholder="Misal: Observasi Febris ec Suspek Demam Dengue..." />
                  </div>
                  <div>
                    <label className="block text-[10px] font-black text-slate-700 dark:text-slate-200 mb-1 flex items-center gap-1 uppercase tracking-widest">ICD-10 / Diagnosis Spesifik</label>
                    <p className="text-[9px] text-slate-500 dark:text-slate-400 mb-2 uppercase font-bold tracking-tight">Kode / Nama penyakit spesifik.</p>
                    <Icd10Search value={soapForm.diagnosis} onChange={val => setSoapForm({...soapForm, diagnosis: val})} required />
                  </div>
                </div>

                <div>
                  <label className="block text-[10px] font-black text-slate-700 dark:text-slate-200 mb-1 flex items-center gap-1 uppercase tracking-widest"><span className="bg-indigo-100 dark:bg-indigo-900 text-indigo-800 dark:text-indigo-300 px-1.5 rounded text-[9px]">P</span> Plan (Rencana Pengobatan)</label>
                  <p className="text-[9px] text-slate-500 dark:text-slate-400 mb-2 uppercase font-bold tracking-tight">Rencana terapi, edukasi, tindakan, atau rujukan.</p>
                  <textarea required value={soapForm.plan} onChange={e => setSoapForm({...soapForm, plan: e.target.value})} className="w-full px-4 py-2.5 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[80px] transition-all shadow-sm" placeholder="Misal: Cek darah lengkap, tirah baring..." />
                </div>
                
                <div className="pt-4 border-t border-slate-100 dark:border-slate-800">
                  <h4 className="text-xs font-black text-indigo-900 dark:text-indigo-400 mb-2 flex items-center gap-2 uppercase tracking-widest transition-colors"><Pill className="w-4 h-4" /> Resep Obat Terintegrasi (CPOE)</h4>
                  <p className="text-[9px] text-slate-500 dark:text-slate-500 mb-4 uppercase font-bold">Resep ini akan langsung tersambung dan mengurangi stok gudang farmasi / kasir.</p>
                  
                  <div className="bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-800 rounded-2xl p-4 mb-4 transition-colors">
                    <div className="grid grid-cols-1 md:grid-cols-4 gap-4 mb-4">
                      <div className="md:col-span-2">
                        <label className="block text-[9px] font-black text-slate-500 dark:text-slate-400 mb-2 uppercase tracking-widest">Pilih Obat (Sisa Stok)</label>
                        <select value={prescriptionForm.drug_id} onChange={e => setPrescriptionForm({...prescriptionForm, drug_id: e.target.value})} className="w-full px-3 py-2 border border-slate-200 dark:border-slate-700 rounded-lg text-[11px] bg-white dark:bg-slate-800 dark:text-white focus:outline-none focus:ring-1 focus:ring-indigo-500 transition-all">
                          <option value="">-- Pilih Obat --</option>
                          {drugsInfo.map(d => (
                            <option key={d.id} value={d.id} disabled={d.stock <= 0}>{d.name} ({d.stock} {d.unit} tersisa)</option>
                          ))}
                        </select>
                      </div>
                      <div>
                        <label className="block text-[9px] font-black text-slate-500 dark:text-slate-400 mb-2 uppercase tracking-widest">Dosis (Aturan Pakai)</label>
                        <input value={prescriptionForm.dosage} onChange={e => setPrescriptionForm({...prescriptionForm, dosage: e.target.value})} type="text" className="w-full px-3 py-2 border border-slate-200 dark:border-slate-700 rounded-lg text-[11px] bg-white dark:bg-slate-800 dark:text-white focus:outline-none focus:ring-1 focus:ring-indigo-500 transition-all font-medium" placeholder="Ex: 3 x 1 Pcs H.P" />
                      </div>
                      <div>
                        <label className="block text-[9px] font-black text-slate-500 dark:text-slate-400 mb-2 uppercase tracking-widest">Jumlah Obat</label>
                        <input value={prescriptionForm.quantity} onChange={e => setPrescriptionForm({...prescriptionForm, quantity: Number(e.target.value)})} type="number" min="1" className="w-full px-3 py-2 border border-slate-200 dark:border-slate-700 rounded-lg text-[11px] bg-white dark:bg-slate-800 dark:text-white focus:outline-none focus:ring-1 focus:ring-indigo-500 transition-all font-mono" placeholder="Qty" />
                      </div>
                    </div>
                    <div>
                      <label className="block text-[9px] font-black text-slate-500 dark:text-slate-400 mb-2 uppercase tracking-widest">Catatan Tambahan (Opsional)</label>
                      <input value={prescriptionForm.notes} onChange={e => setPrescriptionForm({...prescriptionForm, notes: e.target.value})} type="text" className="w-full px-3 py-2 border border-slate-200 dark:border-slate-700 rounded-lg text-[11px] bg-white dark:bg-slate-800 dark:text-white focus:outline-none focus:ring-1 focus:ring-indigo-500 mb-3 transition-all" placeholder="Contoh: Sesudah makan..." />
                    </div>
                    <button type="button" onClick={handleAddPrescription} className="w-full bg-indigo-600 hover:bg-indigo-700 text-white text-[10px] font-black uppercase tracking-widest py-3 rounded-xl transition-all flex items-center justify-center gap-2 shadow-lg shadow-indigo-100 dark:shadow-none">
                      <Plus className="w-4 h-4" /> Tambah Obat Ke Resep
                    </button>
                  </div>
                  
                  {prescriptions.length > 0 && (
                    <div className="border border-emerald-200 dark:border-emerald-900/30 bg-emerald-50/50 dark:bg-emerald-950/20 rounded-2xl overflow-hidden mt-4 transition-colors">
                      <div className="bg-emerald-100/50 dark:bg-emerald-900/40 px-4 py-2.5 border-b border-emerald-200 dark:border-emerald-900/30">
                        <h5 className="text-[9px] font-black text-emerald-800 dark:text-emerald-400 uppercase tracking-widest">Order Farmasi Digital ({prescriptions.length} Item)</h5>
                      </div>
                      <div className="divide-y divide-emerald-100 dark:divide-emerald-900/20">
                        {prescriptions.map((p, idx) => (
                          <div key={p.id || p.temp_id} className="p-4 text-xs flex justify-between items-start hover:bg-white dark:hover:bg-slate-800 transition-colors">
                            <div>
                              <div className="font-black text-slate-800 dark:text-slate-200 flex items-center gap-2 uppercase tracking-tight">
                                <span className="w-5 h-5 flex items-center justify-center bg-emerald-100 dark:bg-emerald-900 text-emerald-600 dark:text-emerald-400 rounded-lg text-[9px] font-black">{idx + 1}</span>
                                {p.drug_name}
                              </div>
                              <div className="text-slate-600 dark:text-slate-400 mt-2 pl-7 flex items-center gap-3">
                                <span className="inline-block bg-slate-100 dark:bg-slate-800 px-2 py-0.5 rounded-lg font-mono text-[10px] font-bold">{p.quantity} {p.drug_unit}</span>
                                <span className="font-black text-[10px] uppercase text-indigo-600 dark:text-indigo-400 tracking-tight">{p.dosage}</span>
                                {p.notes && <span className="italic text-slate-500 dark:text-slate-500 text-[10px] font-bold uppercase tracking-tighter">/ {p.notes}</span>}
                              </div>
                            </div>
                            <button type="button" onClick={() => handleDeletePrescription(p.id || p.temp_id, !p.id)} className="text-red-500 hover:text-red-700 p-2 bg-red-50 dark:bg-red-900/20 hover:bg-red-100 dark:hover:bg-red-900/40 rounded-xl transition-all">
                              <Trash2 className="w-4 h-4" />
                            </button>
                          </div>
                        ))}
                      </div>
                    </div>
                  )}
                  {prescriptions.length === 0 && (
                    <div className="text-center py-8 border-2 border-dashed border-slate-100 dark:border-slate-800 rounded-[24px] text-slate-400 dark:text-slate-600 text-[10px] font-bold uppercase tracking-widest mt-4">
                      Keranjang Resep Kosong
                    </div>
                  )}

                  <div className="mt-6">
                    <label className="block text-[10px] font-black text-slate-700 dark:text-slate-300 mb-1 uppercase tracking-widest">Catatan Resep Manuskrip / Racikan</label>
                    <p className="text-[9px] text-slate-500 dark:text-slate-500 mb-2 uppercase font-bold tracking-tight">Gunakan hanya untuk obat luar stok atau rujukan.</p>
                    <textarea value={soapForm.medication} onChange={e => setSoapForm({...soapForm, medication: e.target.value})} className="w-full px-4 py-3 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[60px] transition-all shadow-sm" placeholder="Misal: Puyer racik batuk pilek 3x1 (k/p)..." />
                  </div>
                </div>

                {editingItem && (editingItem.is_pregnant === 1 || editingItem.is_pregnant === true) && (
                  <div className="pt-4 border-t border-slate-100 dark:border-slate-800">
                    <h4 className="text-xs font-black text-indigo-900 dark:text-indigo-400 mb-2 flex items-center gap-2 uppercase tracking-widest transition-colors"><ImageIcon className="w-4 h-4" /> Hasil Pencitraan / USG (Opsional)</h4>
                    <div className="bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-800 rounded-2xl p-4 transition-colors">
                      <div className="flex flex-col md:flex-row gap-4 mb-4">
                        <div className="flex-1">
                          <textarea value={soapForm.usg_image_notes} onChange={e => setSoapForm({...soapForm, usg_image_notes: e.target.value})} className="w-full px-4 py-3 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-white dark:bg-slate-800 dark:text-white focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[96px] transition-all shadow-sm" placeholder="Catatan hasil USG atau rontgen..." />
                        </div>
                        <div className="shrink-0 w-full md:w-48">
                          <label className="flex flex-col items-center justify-center w-full h-24 border-2 border-indigo-200 dark:border-indigo-800 border-dashed rounded-xl cursor-pointer bg-indigo-50 dark:bg-indigo-900/20 hover:bg-indigo-100 dark:hover:bg-indigo-800/40 transition-colors">
                            <div className="flex flex-col items-center justify-center pt-5 pb-6">
                              <p className="mb-1 text-[10px] text-indigo-600 dark:text-indigo-400"><span className="font-bold">Upload Foto</span> (Bisa &gt;1)</p>
                            </div>
                            <input type="file" multiple className="hidden" accept="image/*" onChange={async (e) => {
                              const files = e.target.files;
                              if (files && files.length > 0) {
                                const compressImage = (file: File): Promise<string> => {
                                  return new Promise((resolve, reject) => {
                                    const reader = new FileReader();
                                    reader.readAsDataURL(file);
                                    reader.onload = (event) => {
                                      const img = new Image();
                                      img.src = event.target?.result as string;
                                      img.onload = () => {
                                        const canvas = document.createElement('canvas');
                                        let width = img.width; let height = img.height;
                                        const MAX = 1200;
                                        if (width > height && width > MAX) { height *= MAX / width; width = MAX; }
                                        else if (height > MAX) { width *= MAX / height; height = MAX; }
                                        canvas.width = width; canvas.height = height;
                                        const ctx = canvas.getContext('2d');
                                        if (ctx) {
                                          ctx.fillStyle = '#FFFFFF';
                                          ctx.fillRect(0, 0, width, height);
                                          ctx.drawImage(img, 0, 0, width, height);
                                        }
                                        resolve(canvas.toDataURL('image/jpeg', 0.8));
                                      };
                                      img.onerror = reject;
                                    };
                                    reader.onerror = reject;
                                  });
                                };
                                try {
                                  const compressed = await Promise.all(Array.from(files).map(compressImage));
                                  let currentImages: string[] = [];
                                  try {
                                    if (soapForm.usg_image) {
                                      if (soapForm.usg_image.startsWith('[')) currentImages = JSON.parse(soapForm.usg_image);
                                      else currentImages = [soapForm.usg_image];
                                    }
                                  } catch(e) { if (soapForm.usg_image) currentImages = [soapForm.usg_image]; }
                                  setSoapForm({...soapForm, usg_image: JSON.stringify([...currentImages, ...compressed])});
                                } catch(e) { console.error("Error compressing", e); }
                              }
                            }} />
                          </label>
                        </div>
                      </div>
                      {soapForm.usg_image && (() => {
                         let images: string[] = [];
                         try { 
                           if (soapForm.usg_image.startsWith('[')) images = JSON.parse(soapForm.usg_image);
                           else images = [soapForm.usg_image];
                         } catch(e) { images = [soapForm.usg_image]; }
                         return (
                           <div className="flex gap-2 overflow-x-auto w-full items-center pb-2">
                             {images.map((imgSrc, idx) => (
                               <div key={idx} className="relative group shrink-0">
                                 <img src={imgSrc} alt="USG" className="h-24 w-24 object-cover rounded-xl border border-indigo-200 dark:border-indigo-800 shadow-sm" />
                                 <button type="button" onClick={() => {
                                   const newArr = [...images]; newArr.splice(idx, 1);
                                   setSoapForm({...soapForm, usg_image: newArr.length ? JSON.stringify(newArr) : ''});
                                 }} className="absolute -top-2 -right-2 bg-red-500 text-white rounded-full p-1 opacity-0 group-hover:opacity-100"><X className="w-3 h-3" /></button>
                               </div>
                             ))}
                           </div>
                         );
                      })()}
                    </div>
                  </div>
                )}
                
                <div className="pt-5 border-t border-slate-100 dark:border-slate-800">
                  <h4 className="text-xs font-black text-indigo-900 dark:text-indigo-400 mb-2 flex items-center gap-2 uppercase tracking-widest"><Activity className="w-4 h-4" /> Digital Lab & Radiology (CPOE)</h4>
                  <p className="text-[9px] text-slate-500 dark:text-slate-500 mb-4 uppercase font-bold">Pesan tes diagnostik secara digital tanpa kertas manual.</p>
                  
                  <div className="space-y-4">
                    <div>
                      <label className="block text-[10px] font-bold text-slate-700 dark:text-slate-300 mb-2 uppercase tracking-widest">Quick Order Lab (Dasar)</label>
                      <div className="flex flex-wrap gap-2">
                        {["Cek Darah Lengkap", "Urine Rutin", "Gula Darah Puasa", "Kolesterol Total", "Asam Urat", "SGOT / SGPT", "Ureum / Kreatinin", "Widal / Typhus", "Dengue (NS1/IgG/IgM)"].map(test => {
                          const isSelected = soapForm.lab_orders.includes(test);
                          return (
                            <button
                              type="button"
                              key={test}
                              onClick={() => {
                                let arr = soapForm.lab_orders.split(',').map(s=>s.trim()).filter(Boolean);
                                if (arr.includes(test)) arr = arr.filter(x => x !== test);
                                else arr.push(test);
                                setSoapForm({...soapForm, lab_orders: arr.join(', ')});
                              }}
                              className={`px-3 py-1.5 rounded-full text-[9px] font-black uppercase transition-all border ${isSelected ? 'bg-indigo-600 dark:bg-indigo-500 text-white border-indigo-600 dark:border-indigo-500 shadow-sm' : 'bg-white dark:bg-slate-800 text-slate-600 dark:text-slate-400 border-slate-200 dark:border-slate-700 hover:border-indigo-300 dark:hover:border-indigo-800 outline-none'}`}
                            >
                              {test}
                            </button>
                          )
                        })}
                      </div>
                    </div>
                    
                    <div>
                      <label className="block text-[10px] font-bold text-slate-700 dark:text-slate-300 mb-2 uppercase tracking-widest">Quick Order Radiologi</label>
                      <div className="flex flex-wrap gap-2">
                        {["Rontgen Thorax", "USG Abdomen", "USG Kehamilan", "CT Scan Kepala", "MRI Tulang Belakang", "EKG (Elektrokardiogram)"].map(test => {
                          const isSelected = soapForm.radiology_orders.includes(test);
                          return (
                            <button
                              type="button"
                              key={test}
                              onClick={() => {
                                let arr = soapForm.radiology_orders.split(',').map(s=>s.trim()).filter(Boolean);
                                if (arr.includes(test)) arr = arr.filter(x => x !== test);
                                else arr.push(test);
                                setSoapForm({...soapForm, radiology_orders: arr.join(', ')});
                              }}
                              className={`px-3 py-1.5 rounded-full text-[9px] font-black uppercase transition-all border ${isSelected ? 'bg-indigo-600 dark:bg-indigo-500 text-white border-indigo-600 dark:border-indigo-500 shadow-sm' : 'bg-white dark:bg-slate-800 text-slate-600 dark:text-slate-400 border-slate-200 dark:border-slate-700 hover:border-indigo-300 dark:hover:border-indigo-800 outline-none'}`}
                            >
                              {test}
                            </button>
                          )
                        })}
                      </div>
                    </div>

                    <div className="grid grid-cols-1 md:grid-cols-2 gap-5 mt-4">
                       <div>
                          <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 mb-2 uppercase tracking-widest">Order Lab (Custom/Lainnya)</label>
                          <textarea value={soapForm.lab_orders} onChange={e => setSoapForm({...soapForm, lab_orders: e.target.value})} className="w-full px-4 py-3 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-100 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[80px] transition-all" placeholder="Misal: DL, GDS, Kolesterol..." />
                       </div>
                       <div>
                          <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 mb-2 uppercase tracking-widest">Order Radiologi (Custom/Lainnya)</label>
                          <textarea value={soapForm.radiology_orders} onChange={e => setSoapForm({...soapForm, radiology_orders: e.target.value})} className="w-full px-4 py-3 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-100 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[80px] transition-all" placeholder="Misal: Thorax PA, USG Abdomen..." />
                       </div>
                    </div>
                  </div>
                </div>

                <div className="pt-5 border-t border-slate-100 dark:border-slate-800">
                  <h4 className="text-xs font-black text-teal-900 dark:text-emerald-400 mb-2 flex items-center gap-2 uppercase tracking-widest"><Activity className="w-4 h-4 text-emerald-600" /> Hasil Diagnostik & Rekomendasi</h4>
                  <p className="text-[9px] text-slate-500 dark:text-slate-500 mb-4 uppercase font-bold tracking-tight">Input data penunjang dan tindak lanjut dokter.</p>
                  
                  <div className="space-y-4">
                    {soapForm.lab_orders && (
                      <div className="mb-3">
                        <label className="block text-[10px] font-black text-slate-700 dark:text-slate-300 mb-1 uppercase tracking-widest">Hasil Laboratorium <span className="text-emerald-600 dark:text-emerald-400 font-bold lowercase italic">/ current orders</span></label>
                        <textarea value={soapForm.lab_results} onChange={e => setSoapForm({...soapForm, lab_results: e.target.value})} className="w-full px-4 py-3 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-emerald-500 min-h-[60px] transition-all" placeholder="Input hasil laboratorium..." />
                      </div>
                    )}

                    {soapForm.radiology_orders && (
                      <div className="mb-3">
                        <label className="block text-[10px] font-black text-slate-700 dark:text-slate-300 mb-1 uppercase tracking-widest">Hasil Radiologi <span className="text-emerald-600 dark:text-emerald-400 font-bold lowercase italic">/ current orders</span></label>
                        <textarea value={soapForm.radiology_results} onChange={e => setSoapForm({...soapForm, radiology_results: e.target.value})} className="w-full px-4 py-3 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-emerald-500 min-h-[60px] transition-all" placeholder="Input hasil radiologi..." />
                      </div>
                    )}

                    <div className="mb-3">
                      <label className="block text-[10px] font-black text-slate-700 dark:text-slate-300 mb-1 uppercase tracking-widest">Edukasi & Rekomendasi Lanjutan</label>
                      <textarea value={soapForm.followup_recommendations} onChange={e => setSoapForm({...soapForm, followup_recommendations: e.target.value})} className="w-full px-4 py-3 border border-slate-200 dark:border-slate-700 rounded-xl text-xs bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:outline-none focus:ring-2 focus:ring-emerald-500 min-h-[80px] transition-all" placeholder="Misal: Kontrol ulang 3 hari lagi, jaga pola makan..." />
                    </div>
                  </div>
                </div>

                <div className="pt-6 flex justify-end gap-3 shrink-0 bg-slate-50 dark:bg-slate-800/50 border-t border-slate-100 dark:border-slate-800 -mx-5 -mb-5 p-6 rounded-b-2xl">
                   <button type="button" onClick={() => setModalType('none')} className="px-6 py-3 bg-slate-100 dark:bg-slate-800 rounded-xl text-[10px] font-black uppercase tracking-widest text-slate-600 dark:text-slate-400 hover:bg-slate-200 dark:hover:bg-slate-700 transition-all outline-none">Batal / Draft</button>
                   <button type="submit" className="px-8 py-3 bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-500 dark:hover:bg-indigo-600 rounded-xl text-[10px] font-black uppercase tracking-widest text-white shadow-lg shadow-indigo-100 dark:shadow-none transition-all outline-none">Finalisasi Rekam Medis (SOAP)</button>
                </div>
              </form>
            </motion.div>
          </motion.div>
        )}
        </AnimatePresence>

        {modalType === 'anc' && (
          <div className="fixed inset-0 bg-slate-900/50 flex items-center justify-center p-4 z-50 backdrop-blur-sm">
            <div className="bg-white dark:bg-slate-900 rounded-2xl w-full max-w-2xl shadow-xl overflow-hidden border border-slate-200 dark:border-slate-800 flex flex-col max-h-[90vh]">
              <div className="flex justify-between items-center p-4 border-b border-pink-100 dark:border-pink-900/30 bg-pink-50 dark:bg-pink-950/20 shrink-0">
                <h3 className="font-bold text-pink-900 dark:text-pink-300 flex items-center gap-2 uppercase tracking-tight text-sm"><Baby className="w-5 h-5 text-pink-600 dark:text-pink-400" /> Pemeriksaan Kehamilan (ANC): {editingItem?.name}</h3>
                <button onClick={() => setModalType('none')} className="text-pink-400 hover:text-pink-600 dark:hover:text-white transition-colors outline-none"><X className="w-5 h-5" /></button>
              </div>
              <form onSubmit={saveAnc} className="p-5 overflow-y-auto flex-grow custom-scrollbar space-y-6 bg-white dark:bg-slate-900">
                <div className="space-y-6">
                  {/* Riwayat Haid & Kandungan */}
                  <div className="space-y-3">
                    <h4 className="text-[10px] font-black text-slate-400 dark:text-slate-500 uppercase tracking-[0.2em] border-b dark:border-slate-800 pb-1.5 flex items-center gap-2">Riwayat & Usia Kandungan</h4>
                    <div className="grid grid-cols-2 gap-4">
                      <VitalInputField 
                        label="HPHT" 
                        unit="Haid Terakhir" 
                        type="date" 
                        value={ancForm.hpht || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, hpht: val, estimated_delivery_date: calculateHPL(val), gestational_age: calculateGestationalAge(val)})} 
                      />
                      <VitalInputField 
                        label="HPL" 
                        unit="Perkiraan Lahir" 
                        type="date" 
                        value={ancForm.estimated_delivery_date || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, estimated_delivery_date: val})} 
                      />
                      <VitalInputField 
                        label="Usia Kandungan" 
                        unit="Minggu" 
                        type="number" 
                        min={1} 
                        max={42} 
                        value={ancForm.gestational_age || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, gestational_age: val})} 
                        placeholder="Mgg" 
                      />
                      <VitalInputField 
                        label="Skor Poedji Rochjati" 
                        unit="Risiko" 
                        type="number" 
                        value={ancForm.poedji_rochjati_score || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, poedji_rochjati_score: val})} 
                        placeholder="Contoh: 2" 
                      />
                    </div>
                  </div>

                  {/* Pemeriksaan Fisik Ibu & Janin */}
                  <div className="space-y-3">
                    <h4 className="text-[10px] font-black text-slate-400 dark:text-slate-500 uppercase tracking-[0.2em] border-b dark:border-slate-800 pb-1.5 flex items-center gap-2">Pemeriksaan Fisik Janin</h4>
                    <div className="grid grid-cols-2 gap-4">
                      <VitalInputField 
                        label="Tinggi Fundus Uteri" 
                        unit="TFU" 
                        value={ancForm.tfu || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, tfu: val})} 
                        placeholder="Contoh: 28 cm" 
                      />
                      <VitalInputField 
                        label="Detak Jantung Janin" 
                        unit="DJJ" 
                        type="number" 
                        value={ancForm.djj || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, djj: val})} 
                        placeholder="x / Menit" 
                      />
                    </div>
                  </div>

                  {/* Pemeriksaan Leopold */}
                  <div className="space-y-3 bg-pink-50/50 dark:bg-pink-950/10 p-4 rounded-xl border border-pink-100 dark:border-pink-900/30 shadow-sm">
                    <h4 className="text-[10px] font-black text-pink-900 dark:text-pink-400 border-b border-pink-200 dark:border-pink-900/30 pb-1.5 uppercase tracking-widest">Palpasi Leopold</h4>
                    <div className="grid grid-cols-2 gap-4">
                      <VitalInputField 
                        label="Leopold 1" 
                        unit="Fundus" 
                        value={ancForm.leopold_1 || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, leopold_1: val})} 
                        placeholder="Bokong/Kepala/Kosong" 
                        className="leopold-input"
                      />
                      <VitalInputField 
                        label="Leopold 2" 
                        unit="Samping" 
                        value={ancForm.leopold_2 || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, leopold_2: val})} 
                        placeholder="Punggung (PUKA/PUKI)" 
                      />
                      <VitalInputField 
                        label="Leopold 3" 
                        unit="Bawah" 
                        value={ancForm.leopold_3 || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, leopold_3: val})} 
                        placeholder="Kepala/Bokong" 
                      />
                      <VitalInputField 
                        label="Leopold 4" 
                        unit="Masuk PAP" 
                        value={ancForm.leopold_4 || ''} 
                        onChange={(val: any) => setAncForm({...ancForm, leopold_4: val})} 
                        placeholder="Divergen/Konvergen" 
                      />
                    </div>
                  </div>


                  {/* Hasil USG */}
                  <div className="space-y-3 bg-indigo-50/50 dark:bg-indigo-950/10 p-4 rounded-xl border border-indigo-100 dark:border-indigo-900/30 shadow-sm mt-4">
                    <h4 className="text-[10px] font-black text-indigo-900 dark:text-indigo-400 border-b border-indigo-200 dark:border-indigo-900/30 pb-1.5 uppercase tracking-widest flex items-center justify-between">
                      <span>Pemeriksaan USG Ultrasonografi</span>
                      <span className="text-[9px] bg-indigo-200 dark:bg-indigo-800 text-indigo-700 dark:text-indigo-300 px-1.5 py-0.5 rounded">Opsional</span>
                    </h4>
                    
                    <div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mt-3">
                      <VitalInputField
                        label="BPD (Kepala)"
                        unit="mm"
                        value={ancForm.usg_bpd || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_bpd: val})}
                        placeholder="Biparietal"
                      />
                      <VitalInputField
                        label="HC (Lingkar)"
                        unit="mm"
                        value={ancForm.usg_hc || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_hc: val})}
                        placeholder="Head Circ."
                      />
                      <VitalInputField
                        label="AC (Perut)"
                        unit="mm"
                        value={ancForm.usg_ac || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_ac: val})}
                        placeholder="Abdomen"
                      />
                      <VitalInputField
                        label="FL (Paha)"
                        unit="mm"
                        value={ancForm.usg_fl || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_fl: val})}
                        placeholder="Femur Length"
                      />
                      <VitalInputField
                        label="EFW / TBJ"
                        unit="Gram"
                        value={ancForm.usg_tbj || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_tbj: val})}
                        placeholder="Tapsiran Berat"
                      />
                      <VitalInputField
                        label="AFI (Ketuban)"
                        unit="cm / Ket"
                        value={ancForm.usg_afi || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_afi: val})}
                        placeholder="Indeks Ketuban"
                      />
                      <VitalInputField
                        label="Letak Plasenta"
                        value={ancForm.usg_placenta || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_placenta: val})}
                        placeholder="Fundus/Previa/dll"
                      />
                      <VitalInputField
                        label="Presentasi"
                        value={ancForm.usg_presentation || ''}
                        onChange={(val: any) => setAncForm({...ancForm, usg_presentation: val})}
                        placeholder="Kepala/Bokong"
                      />
                    </div>
                    
                    <div className="mt-4">
                      <label className="block text-[10px] font-black text-indigo-900 dark:text-indigo-400 uppercase tracking-widest mb-1.5 flex items-center gap-2">Catatan Hasil USG</label>
                      <textarea value={ancForm.usg_image_notes || ''} onChange={e => setAncForm({...ancForm, usg_image_notes: e.target.value})} className="w-full px-4 py-3 border border-indigo-200 dark:border-indigo-800 rounded-xl text-xs bg-white dark:bg-slate-800 dark:text-white focus:outline-none focus:ring-2 focus:ring-indigo-500 min-h-[60px] transition-all shadow-sm" placeholder="Interpretasi atau catatan dari hasil USG..." />
                    </div>

                    <div className="mt-4">
                      <label className="block text-[10px] font-black text-indigo-900 dark:text-indigo-400 uppercase tracking-widest mb-1.5 flex items-center gap-2">
                         Lampiran Foto USG
                      </label>
                      <div className="flex flex-col sm:flex-row gap-4 items-start">
                        <div className="flex-1 w-full relative">
                          <label className="flex flex-col items-center justify-center w-full h-24 border-2 border-indigo-200 dark:border-indigo-800 border-dashed rounded-xl cursor-pointer bg-indigo-50 dark:bg-indigo-900/20 hover:bg-indigo-100 dark:hover:bg-indigo-800/40 transition-colors">
                            <div className="flex flex-col items-center justify-center pt-5 pb-6">
                              <svg className="w-6 h-6 mb-2 text-indigo-500 dark:text-indigo-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 16">
                                <path stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M13 13h3a3 3 0 0 0 0-6h-.025A5.56 5.56 0 0 0 16 6.5 5.5 5.5 0 0 0 5.207 5.021C5.137 5.017 5.071 5 5 5a4 4 0 0 0 0 8h2.167M10 15V6m0 0L8 8m2-2 2 2"/>
                              </svg>
                              <p className="mb-1 text-[10px] text-indigo-600 dark:text-indigo-400"><span className="font-bold">Tekan untuk upload</span> foto USG</p>
                              <p className="text-[9px] text-indigo-400 dark:text-indigo-500">Bisa pilih lebih dari satu</p>
                            </div>
                            <input type="file" multiple className="hidden" accept="image/*" onChange={async (e) => {
                              const files = e.target.files;
                              if (files && files.length > 0) {
                                const compressImage = (file: File): Promise<string> => {
                                  return new Promise((resolve, reject) => {
                                    const reader = new FileReader();
                                    reader.readAsDataURL(file);
                                    reader.onload = (event) => {
                                      const img = new Image();
                                      img.src = event.target?.result as string;
                                      img.onload = () => {
                                        const canvas = document.createElement('canvas');
                                        let width = img.width;
                                        let height = img.height;
                                        const MAX = 1200;
                                        if (width > height && width > MAX) { height *= MAX / width; width = MAX; }
                                        else if (height > MAX) { width *= MAX / height; height = MAX; }
                                        canvas.width = width; canvas.height = height;
                                        const ctx = canvas.getContext('2d');
                                        if (ctx) {
                                          ctx.fillStyle = '#FFFFFF';
                                          ctx.fillRect(0, 0, width, height);
                                          ctx.drawImage(img, 0, 0, width, height);
                                        }
                                        resolve(canvas.toDataURL('image/jpeg', 0.8));
                                      };
                                      img.onerror = reject;
                                    };
                                    reader.onerror = reject;
                                  });
                                };
                                
                                try {
                                  const compressedImages = await Promise.all(Array.from(files).map(compressImage));
                                  let currentImages: string[] = [];
                                  try {
                                    if (ancForm.usg_image) {
                                      if (ancForm.usg_image.startsWith('[')) {
                                        currentImages = JSON.parse(ancForm.usg_image);
                                      } else {
                                        currentImages = [ancForm.usg_image];
                                      }
                                    }
                                  } catch (err) {
                                    if (ancForm.usg_image) currentImages = [ancForm.usg_image];
                                  }
                                  
                                  const allImages = [...currentImages, ...compressedImages];
                                  setAncForm({...ancForm, usg_image: JSON.stringify(allImages)});
                                } catch (error) {
                                  console.error("Gagal kompres gambar", error);
                                }
                              }
                            }} />
                          </label>
                        </div>
                        {ancForm.usg_image && (() => {
                           let images: string[] = [];
                           try { 
                             if (ancForm.usg_image.startsWith('[')) {
                               images = JSON.parse(ancForm.usg_image);
                             } else {
                               images = [ancForm.usg_image];
                             }
                           } catch(e) { images = [ancForm.usg_image]; }
                           
                           return (
                             <div className="flex gap-2 overflow-x-auto shrink-0 max-w-[50%] items-center">
                               {images.map((imgSrc, idx) => (
                                 <div key={idx} className="relative group shrink-0">
                                   <img src={imgSrc} alt="USG" className="h-24 w-24 object-cover rounded-xl border border-indigo-200 dark:border-indigo-800 shadow-sm" />
                                   <button 
                                     type="button" 
                                     onClick={() => {
                                       const newArr = [...images];
                                       newArr.splice(idx, 1);
                                       setAncForm({...ancForm, usg_image: newArr.length ? JSON.stringify(newArr) : ''});
                                     }}
                                     className="absolute -top-2 -right-2 bg-red-500 hover:bg-red-600 text-white rounded-full p-1 shadow-md opacity-0 group-hover:opacity-100 transition-opacity"
                                   >
                                     <X className="w-3 h-3" />
                                   </button>
                                 </div>
                               ))}
                             </div>
                           );
                        })()}
                      </div>
                    </div>
                  </div>

                  {/* Catatan & Tindak Lanjut */}
                  <div className="space-y-3">
                    <h4 className="text-[10px] font-black text-slate-400 dark:text-slate-500 uppercase tracking-[0.2em] border-b dark:border-slate-800 pb-1.5 flex items-center gap-2">Tindak Lanjut</h4>
                    <div>
                      <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-1.5">Catatan Tambahan & Terapi</label>
                      <textarea value={ancForm.fetal_development || ''} onChange={e => setAncForm({...ancForm, fetal_development: e.target.value})} rows={3} className="w-full px-3 py-2 border border-slate-200 dark:border-slate-800 rounded-lg text-sm bg-slate-50 dark:bg-slate-800 dark:text-white focus:bg-white dark:focus:bg-slate-750 focus:ring-2 focus:ring-pink-500 outline-none transition-all placeholder:text-slate-400 dark:placeholder:text-slate-600" placeholder="Kondisi kesehatan ibu, vitamin, dll..." />
                    </div>
                    
                    <div>
                      <label className="block text-[10px] font-black text-slate-500 dark:text-slate-400 uppercase tracking-widest mb-1.5">Jadwal Cek Up Berikutnya</label>
                      <input value={ancForm.next_checkup_date || ''} onChange={e => setAncForm({...ancForm, next_checkup_date: e.target.value})} type="date" className="w-full px-3 py-2 border border-slate-200 dark:border-slate-800 rounded-lg text-sm bg-slate-50 dark:bg-slate-800 dark:text-white focus:ring-2 focus:ring-pink-500 outline-none transition-all" />
                    </div>
                  </div>
                </div>

                <div className="pt-6 border-t dark:border-slate-800 flex justify-end gap-3 mt-4 shrink-0">
                  <button type="button" onClick={() => setModalType('none')} className="px-5 py-2.5 border border-slate-200 dark:border-slate-800 rounded-xl text-[11px] font-bold uppercase tracking-widest text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors">Batal</button>
                  <button type="submit" className="px-5 py-2.5 bg-pink-600 hover:bg-pink-700 dark:bg-pink-500 dark:hover:bg-pink-600 rounded-xl text-[11px] font-bold uppercase tracking-widest text-white shadow-lg shadow-pink-200 dark:shadow-none transition-all">
                    Simpan Data ANC
                  </button>
                </div>
              </form>
            </div>
          </div>
    